"""numpy references for `powermarketjax.solvers` (the L2 numerical-equivalence layer).

Mirrors the package layout: what is model-agnostic in the implementation is
model-agnostic here too, so that no market's reference has to import from
another market's.
"""
